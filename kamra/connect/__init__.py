"""Kamra Connect client: links a self-hosted Kamra to Kamra's hosted
services (advisories, uptime monitoring, encrypted offsite backups,
benchmarks, AI credits). Does nothing until an owner links the install.

The service side is a separate app on hub.kamrapms.com and is reached
over HTTP only. Connect sells services that cost money to run; Kamra's
features are never gated by it.
"""
